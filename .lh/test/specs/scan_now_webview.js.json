{
    "sourceFile": "test/specs/scan_now_webview.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 20,
            "patches": [
                {
                    "date": 1763031756518,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1763032881954,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,24 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, and click Auth0', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(3000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        // Switch to the webview context\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+\r\n+            // Click the Auth0 button in the webview (native view selector)\r\n+            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n+            await auth0Button.click();\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763032893994,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -21,28 +21,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, and click Auth0', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(3000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        // Switch to the webview context\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-\r\n-            // Click the Auth0 button in the webview (native view selector)\r\n-            const auth0Button = await $('//android.widget.TextView[@text=\"Auth0\"]');\r\n-            await auth0Button.click();\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763033840549,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,37 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(3000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        // Switch to the webview context\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+\r\n+            // Click the Auth0 button in the webview (native view selector)\r\n+            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter email\r\n+            const emailField = await $('//input[@type=\"email\"]');\r\n+            await emailField.waitForDisplayed({ timeout: 5000 });\r\n+            await emailField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Enter password\r\n+            const passwordField = await $('//input[@type=\"password\"]');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763034183920,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,40 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(5000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(3000);\r\n+\r\n+            // Wait for Auth0 button to be displayed and click\r\n+            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 5000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter email\r\n+            const emailField = await $('//input[@type=\"email\"]');\r\n+            await emailField.waitForDisplayed({ timeout: 5000 });\r\n+            await emailField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Enter password\r\n+            const passwordField = await $('//input[@type=\"password\"]');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763034196907,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -37,65 +37,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(3000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        // Switch to the webview context\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-\r\n-            // Click the Auth0 button in the webview (native view selector)\r\n-            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter email\r\n-            const emailField = await $('//input[@type=\"email\"]');\r\n-            await emailField.waitForDisplayed({ timeout: 5000 });\r\n-            await emailField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-            // Enter password\r\n-            const passwordField = await $('//input[@type=\"password\"]');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, and click Auth0', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(3000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        // Switch to the webview context\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-\r\n-            // Click the Auth0 button in the webview (native view selector)\r\n-            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n-            await auth0Button.click();\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763034400387,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,40 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(5000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(3000);\r\n+\r\n+            // Wait for Auth0 button to be displayed and click\r\n+            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 5000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username\r\n+            const usernameField = await $('//android.widget.EditText[@resource-id=\"username\"]');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Enter password\r\n+            const passwordField = await $('//input[@type=\"password\"]');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763034796061,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,22 +4,31 @@\n         const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n         await scanNowButton.click();\r\n \r\n         // Wait for the next page/webview to load\r\n-        await driver.pause(5000);\r\n+        await driver.pause(7000);\r\n \r\n         // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n         const contexts = await driver.getContexts();\r\n         const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n         console.log('Available contexts:', contexts);\r\n         console.log('Current context:', await driver.getContext());\r\n         if (webviewContext) {\r\n             await driver.switchContext(webviewContext);\r\n-            await driver.pause(3000);\r\n+            await driver.pause(5000);\r\n \r\n-            // Wait for Auth0 button to be displayed and click\r\n-            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n-            await auth0Button.waitForDisplayed({ timeout: 5000 });\r\n+            // Print page source for debugging\r\n+            console.log(await driver.getPageSource());\r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button;\r\n+            try {\r\n+                auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            } catch (e) {\r\n+                auth0Button = await $('//button[contains(., \"Auth0\")]');\r\n+                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            }\r\n             await auth0Button.click();\r\n \r\n             // Wait for login form to appear\r\n             await driver.pause(2000);\r\n@@ -37,44 +46,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(5000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        console.log('Available contexts:', contexts);\r\n-        console.log('Current context:', await driver.getContext());\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-            await driver.pause(3000);\r\n-\r\n-            // Wait for Auth0 button to be displayed and click\r\n-            const auth0Button = await $('//*[text()=\"Auth0\"]');\r\n-            await auth0Button.waitForDisplayed({ timeout: 5000 });\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter email\r\n-            const emailField = await $('//input[@type=\"email\"]');\r\n-            await emailField.waitForDisplayed({ timeout: 5000 });\r\n-            await emailField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-            // Enter password\r\n-            const passwordField = await $('//input[@type=\"password\"]');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763035229207,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -32,15 +32,15 @@\n \r\n             // Wait for login form to appear\r\n             await driver.pause(2000);\r\n \r\n-            // Enter username\r\n-            const usernameField = await $('//android.widget.EditText[@resource-id=\"username\"]');\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n             await usernameField.waitForDisplayed({ timeout: 5000 });\r\n             await usernameField.setValue('nisitha.prakash@avid.com');\r\n \r\n             // Enter password\r\n-            const passwordField = await $('//input[@type=\"password\"]');\r\n+            const passwordField = await $('input[type=\"password\"]');\r\n             await passwordField.waitForDisplayed({ timeout: 5000 });\r\n             await passwordField.setValue('S3cretPa$$');\r\n         } else {\r\n             throw new Error('WEBVIEW context not found');\r\n"
                },
                {
                    "date": 1763035559070,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -37,11 +37,19 @@\n             const usernameField = await $('#username');\r\n             await usernameField.waitForDisplayed({ timeout: 5000 });\r\n             await usernameField.setValue('nisitha.prakash@avid.com');\r\n \r\n-            // Enter password\r\n-            const passwordField = await $('input[type=\"password\"]');\r\n+            // If a popup appears, tap on the center of the screen to dismiss it\r\n+            try {\r\n+                await driver.touchAction({ action: 'tap', x: 300, y: 600 });\r\n+            } catch (e) {\r\n+                // Ignore if tap fails\r\n+            }\r\n+\r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n             await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n             await passwordField.setValue('S3cretPa$$');\r\n         } else {\r\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n"
                },
                {
                    "date": 1763035754425,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,58 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            // Print page source for debugging\r\n+            console.log(await driver.getPageSource());\r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button;\r\n+            try {\r\n+                auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            } catch (e) {\r\n+                auth0Button = await $('//button[contains(., \"Auth0\")]');\r\n+                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            }\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Swipe down to dismiss the \"Use saved password?\" popup\r\n+            await driver.touchAction([\r\n+                { action: 'press', x: 300, y: 400 },\r\n+                { action: 'moveTo', x: 300, y: 900 },\r\n+                'release'\r\n+            ]);\r\n+            await driver.pause(1000);\r\n+\r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763037773845,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,55 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            // Print page source for debugging\r\n+            console.log(await driver.getPageSource());\r\n+\r\n+            // Hide the keyboard before clicking Auth0\r\n+            await driver.hideKeyboard();\r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Swipe down to dismiss the \"Use saved password?\" popup\r\n+            await driver.touchAction([\r\n+                { action: 'press', x: 300, y: 400 },\r\n+                { action: 'moveTo', x: 300, y: 900 },\r\n+                'release'\r\n+            ]);\r\n+            await driver.pause(1000);\r\n+\r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763038303699,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,55 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for webview context to appear (retry for up to 10s)\r\n+        let webviewContext = null;\r\n+        await browser.waitUntil(async () => {\r\n+            const contexts = await driver.getContexts();\r\n+            webviewContext = contexts.find(c => c.startsWith('WEBVIEW'));\r\n+            return !!webviewContext;\r\n+        }, {\r\n+            timeout: 10000,\r\n+            timeoutMsg: 'Webview context not found after 10s'\r\n+        });\r\n+\r\n+        // Switch to webview context\r\n+        await driver.switchContext(webviewContext);\r\n+        await driver.pause(2000);\r\n+\r\n+        // Print page source for debugging\r\n+        console.log(await driver.getPageSource());\r\n+\r\n+        // Hide the keyboard before clicking Auth0\r\n+        await driver.hideKeyboard();\r\n+\r\n+        // Try flexible XPath for Auth0 button\r\n+        let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+        await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+        await auth0Button.click();\r\n+\r\n+        // Wait for login form to appear\r\n+        await driver.pause(2000);\r\n+\r\n+        // Enter username (email address)\r\n+        const usernameField = await $('#username');\r\n+        await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+        await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+        // Swipe down to dismiss the \"Use saved password?\" popup\r\n+        await driver.touchAction([\r\n+            { action: 'press', x: 300, y: 400 },\r\n+            { action: 'moveTo', x: 300, y: 900 },\r\n+            'release'\r\n+        ]);\r\n+        await driver.pause(1000);\r\n+\r\n+        // Click and enter password\r\n+        const passwordField = await $('#password');\r\n+        await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+        await passwordField.click();\r\n+        await passwordField.setValue('S3cretPa$$');\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763038593781,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,63 +3,8 @@\n         // Click the SCAN NOW button\r\n         const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n         await scanNowButton.click();\r\n \r\n-        // Wait for webview context to appear (retry for up to 10s)\r\n-        let webviewContext = null;\r\n-        await browser.waitUntil(async () => {\r\n-            const contexts = await driver.getContexts();\r\n-            webviewContext = contexts.find(c => c.startsWith('WEBVIEW'));\r\n-            return !!webviewContext;\r\n-        }, {\r\n-            timeout: 10000,\r\n-            timeoutMsg: 'Webview context not found after 10s'\r\n-        });\r\n-\r\n-        // Switch to webview context\r\n-        await driver.switchContext(webviewContext);\r\n-        await driver.pause(2000);\r\n-\r\n-        // Print page source for debugging\r\n-        console.log(await driver.getPageSource());\r\n-\r\n-        // Hide the keyboard before clicking Auth0\r\n-        await driver.hideKeyboard();\r\n-\r\n-        // Try flexible XPath for Auth0 button\r\n-        let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n-        await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-        await auth0Button.click();\r\n-\r\n-        // Wait for login form to appear\r\n-        await driver.pause(2000);\r\n-\r\n-        // Enter username (email address)\r\n-        const usernameField = await $('#username');\r\n-        await usernameField.waitForDisplayed({ timeout: 5000 });\r\n-        await usernameField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-        // Swipe down to dismiss the \"Use saved password?\" popup\r\n-        await driver.touchAction([\r\n-            { action: 'press', x: 300, y: 400 },\r\n-            { action: 'moveTo', x: 300, y: 900 },\r\n-            'release'\r\n-        ]);\r\n-        await driver.pause(1000);\r\n-\r\n-        // Click and enter password\r\n-        const passwordField = await $('#password');\r\n-        await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-        await passwordField.click();\r\n-        await passwordField.setValue('S3cretPa$$');\r\n-    });\r\n-});\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n         // Wait for the next page/webview to load\r\n         await driver.pause(7000);\r\n \r\n         // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n@@ -107,119 +52,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(7000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        console.log('Available contexts:', contexts);\r\n-        console.log('Current context:', await driver.getContext());\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-            await driver.pause(5000);\r\n-\r\n-            // Print page source for debugging\r\n-            console.log(await driver.getPageSource());\r\n-\r\n-            // Try flexible XPath for Auth0 button\r\n-            let auth0Button;\r\n-            try {\r\n-                auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n-                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            } catch (e) {\r\n-                auth0Button = await $('//button[contains(., \"Auth0\")]');\r\n-                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            }\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter username (email address)\r\n-            const usernameField = await $('#username');\r\n-            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n-            await usernameField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-            // Swipe down to dismiss the \"Use saved password?\" popup\r\n-            await driver.touchAction([\r\n-                { action: 'press', x: 300, y: 400 },\r\n-                { action: 'moveTo', x: 300, y: 900 },\r\n-                'release'\r\n-            ]);\r\n-            await driver.pause(1000);\r\n-\r\n-            // Click and enter password\r\n-            const passwordField = await $('#password');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.click();\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(7000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        console.log('Available contexts:', contexts);\r\n-        console.log('Current context:', await driver.getContext());\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-            await driver.pause(5000);\r\n-\r\n-            // Print page source for debugging\r\n-            console.log(await driver.getPageSource());\r\n-\r\n-            // Try flexible XPath for Auth0 button\r\n-            let auth0Button;\r\n-            try {\r\n-                auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n-                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            } catch (e) {\r\n-                auth0Button = await $('//button[contains(., \"Auth0\")]');\r\n-                await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            }\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter username (email address)\r\n-            const usernameField = await $('#username');\r\n-            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n-            await usernameField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-            // If a popup appears, tap on the center of the screen to dismiss it\r\n-            try {\r\n-                await driver.touchAction({ action: 'tap', x: 300, y: 600 });\r\n-            } catch (e) {\r\n-                // Ignore if tap fails\r\n-            }\r\n-\r\n-            // Click and enter password\r\n-            const passwordField = await $('#password');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.click();\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763038659943,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,55 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            // Print page source for debugging\r\n+            console.log(await driver.getPageSource());\r\n+\r\n+            // Hide the keyboard before clicking Auth0\r\n+            //await driver.hideKeyboard();\r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+            // Swipe down to dismiss the \"Use saved password?\" popup\r\n+            await driver.touchAction([\r\n+                { action: 'press', x: 300, y: 400 },\r\n+                { action: 'moveTo', x: 300, y: 900 },\r\n+                'release'\r\n+            ]);\r\n+            await driver.pause(1000);\r\n+\r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763038725150,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -34,16 +34,9 @@\n             const usernameField = await $('#username');\r\n             await usernameField.waitForDisplayed({ timeout: 5000 });\r\n             await usernameField.setValue('nisitha.prakash@avid.com');\r\n \r\n-            // Swipe down to dismiss the \"Use saved password?\" popup\r\n-            await driver.touchAction([\r\n-                { action: 'press', x: 300, y: 400 },\r\n-                { action: 'moveTo', x: 300, y: 900 },\r\n-                'release'\r\n-            ]);\r\n-            await driver.pause(1000);\r\n-\r\n+           \r\n             // Click and enter password\r\n             const passwordField = await $('#password');\r\n             await passwordField.waitForDisplayed({ timeout: 5000 });\r\n             await passwordField.click();\r\n@@ -52,59 +45,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(7000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        console.log('Available contexts:', contexts);\r\n-        console.log('Current context:', await driver.getContext());\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-            await driver.pause(5000);\r\n-\r\n-            // Print page source for debugging\r\n-            console.log(await driver.getPageSource());\r\n-\r\n-            // Hide the keyboard before clicking Auth0\r\n-            await driver.hideKeyboard();\r\n-\r\n-            // Try flexible XPath for Auth0 button\r\n-            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n-            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter username (email address)\r\n-            const usernameField = await $('#username');\r\n-            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n-            await usernameField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-            // Swipe down to dismiss the \"Use saved password?\" popup\r\n-            await driver.touchAction([\r\n-                { action: 'press', x: 300, y: 400 },\r\n-                { action: 'moveTo', x: 300, y: 900 },\r\n-                'release'\r\n-            ]);\r\n-            await driver.pause(1000);\r\n-\r\n-            // Click and enter password\r\n-            const passwordField = await $('#password');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.click();\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763038948280,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,54 @@\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            // Print page source for debugging\r\n+            console.log(await driver.getPageSource());\r\n+\r\n+            // Hide the keyboard before clicking Auth0\r\n+            //await driver.hideKeyboard();\r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+           \r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+\r\n+            // Click the Continue button in webview context\r\n+            // Try XPath for button with text 'Continue'\r\n+            const continueBtnWeb = await $('//button[contains(text(),\"Continue\")]');\r\n+            await continueBtnWeb.waitForDisplayed({ timeout: 7000 });\r\n+            await continueBtnWeb.click();\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763039054147,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -15,11 +15,9 @@\n         if (webviewContext) {\r\n             await driver.switchContext(webviewContext);\r\n             await driver.pause(5000);\r\n \r\n-            // Print page source for debugging\r\n-            console.log(await driver.getPageSource());\r\n-\r\n+            \r\n             // Hide the keyboard before clicking Auth0\r\n             //await driver.hideKeyboard();\r\n \r\n             // Try flexible XPath for Auth0 button\r\n@@ -51,52 +49,4 @@\n             throw new Error('WEBVIEW context not found');\r\n         }\r\n     });\r\n });\r\n-describe('Scan Now and Auth0 WebView Test', () => {\r\n-    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n-        // Click the SCAN NOW button\r\n-        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n-        await scanNowButton.click();\r\n-\r\n-        // Wait for the next page/webview to load\r\n-        await driver.pause(7000);\r\n-\r\n-        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n-        const contexts = await driver.getContexts();\r\n-        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n-        console.log('Available contexts:', contexts);\r\n-        console.log('Current context:', await driver.getContext());\r\n-        if (webviewContext) {\r\n-            await driver.switchContext(webviewContext);\r\n-            await driver.pause(5000);\r\n-\r\n-            // Print page source for debugging\r\n-            console.log(await driver.getPageSource());\r\n-\r\n-            // Hide the keyboard before clicking Auth0\r\n-            //await driver.hideKeyboard();\r\n-\r\n-            // Try flexible XPath for Auth0 button\r\n-            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n-            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n-            await auth0Button.click();\r\n-\r\n-            // Wait for login form to appear\r\n-            await driver.pause(2000);\r\n-\r\n-            // Enter username (email address)\r\n-            const usernameField = await $('#username');\r\n-            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n-            await usernameField.setValue('nisitha.prakash@avid.com');\r\n-\r\n-           \r\n-            // Click and enter password\r\n-            const passwordField = await $('#password');\r\n-            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n-            await passwordField.click();\r\n-            await passwordField.setValue('S3cretPa$$');\r\n-        } else {\r\n-            throw new Error('WEBVIEW context not found');\r\n-        }\r\n-    });\r\n-});\r\n"
                },
                {
                    "date": 1763039074874,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,10 +16,8 @@\n             await driver.switchContext(webviewContext);\r\n             await driver.pause(5000);\r\n \r\n             \r\n-            // Hide the keyboard before clicking Auth0\r\n-            //await driver.hideKeyboard();\r\n \r\n             // Try flexible XPath for Auth0 button\r\n             let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n             await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n"
                },
                {
                    "date": 1763041388143,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,89 @@\n+describe('Quick Add Story Test', () => {\r\n+    it('should click Quick add and select Story from popup', async () => {\r\n+        // Click the Quick add (+) button in native app\r\n+        const quickAddBtn = await $('~Quick add');\r\n+        await quickAddBtn.waitForDisplayed({ timeout: 5000 });\r\n+        await quickAddBtn.click();\r\n+\r\n+});\r\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            \r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+           \r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+\r\n+            // Click the Continue button in webview context\r\n+            // Try XPath for button with text 'Continue'\r\n+            const continueBtnWeb = await $('//button[contains(text(),\"Continue\")]');\r\n+            await continueBtnWeb.waitForDisplayed({ timeout: 7000 });\r\n+            await continueBtnWeb.click();\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n+\r\n+describe('Logout Test', () => {\r\n+    it('should log out and return to login screen', async () => {\r\n+        try {\r\n+            // Step 1: Click the profile icon (native context)\r\n+            const profileIcon = await $('android=new UiSelector().className(\"android.widget.TextView\").instance(1)');\r\n+            await profileIcon.waitForDisplayed({ timeout: 7000 });\r\n+            await profileIcon.click();\r\n+\r\n+            // Step 2: Scroll up to reveal the Logout button\r\n+            await driver.touchAction([\r\n+                { action: 'press', x: 300, y: 900 },\r\n+                { action: 'moveTo', x: 300, y: 400 },\r\n+                'release'\r\n+            ]);\r\n+            await driver.pause(1000);\r\n+\r\n+\r\n+            // Step 3: Click the Logout button using accessibility id (native context)\r\n+            const logoutBtn = await $('~Log out');\r\n+            await logoutBtn.waitForDisplayed({ timeout: 5000 });\r\n+            await logoutBtn.click();\r\n+\r\n+            // Optionally, verify login screen is displayed\r\n+            const loginScreen = await $('//*[contains(text(),\"Login\")]');\r\n+            await loginScreen.waitForDisplayed({ timeout: 7000 });\r\n+        } catch (err) {\r\n+            console.log('Logout button not found or already logged out.');\r\n+        }\r\n+    });\r\n+});\r\n"
                },
                {
                    "date": 1763042298322,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,52 @@\n+   \r\n+describe('Scan Now and Auth0 WebView Test', () => {\r\n+    it('should click SCAN NOW, switch to webview, click Auth0, and enter credentials', async () => {\r\n+        // Click the SCAN NOW button\r\n+        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n+        await scanNowButton.click();\r\n+\r\n+        // Wait for the next page/webview to load\r\n+        await driver.pause(7000);\r\n+\r\n+        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n+        const contexts = await driver.getContexts();\r\n+        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n+        console.log('Available contexts:', contexts);\r\n+        console.log('Current context:', await driver.getContext());\r\n+        if (webviewContext) {\r\n+            await driver.switchContext(webviewContext);\r\n+            await driver.pause(5000);\r\n+\r\n+            \r\n+\r\n+            // Try flexible XPath for Auth0 button\r\n+            let auth0Button = await $('//*[contains(text(),\"Auth0\")]');\r\n+            await auth0Button.waitForDisplayed({ timeout: 7000 });\r\n+            await auth0Button.click();\r\n+\r\n+            // Wait for login form to appear\r\n+            await driver.pause(2000);\r\n+\r\n+            // Enter username (email address)\r\n+            const usernameField = await $('#username');\r\n+            await usernameField.waitForDisplayed({ timeout: 5000 });\r\n+            await usernameField.setValue('nisitha.prakash@avid.com');\r\n+\r\n+           \r\n+            // Click and enter password\r\n+            const passwordField = await $('#password');\r\n+            await passwordField.waitForDisplayed({ timeout: 5000 });\r\n+            await passwordField.click();\r\n+            await passwordField.setValue('S3cretPa$$');\r\n+\r\n+            // Click the Continue button in webview context\r\n+            // Try XPath for button with text 'Continue'\r\n+            const continueBtnWeb = await $('//button[contains(text(),\"Continue\")]');\r\n+            await continueBtnWeb.waitForDisplayed({ timeout: 7000 });\r\n+            await continueBtnWeb.click();\r\n+        } else {\r\n+            throw new Error('WEBVIEW context not found');\r\n+        }\r\n+    });\r\n+});\r\n+\r\n"
                }
            ],
            "date": 1763031756518,
            "name": "Commit-0",
            "content": "describe('Scan Now and Auth0 WebView Test', () => {\r\n    it('should click SCAN NOW, switch to webview, and click Auth0', async () => {\r\n        // Click the SCAN NOW button\r\n        const scanNowButton = await $('android=new UiSelector().text(\"SCAN NOW\")');\r\n        await scanNowButton.click();\r\n\r\n        // Wait for the next page/webview to load\r\n        await driver.pause(3000);\r\n\r\n        // Get all contexts (NATIVE_APP, WEBVIEW, etc.)\r\n        const contexts = await driver.getContexts();\r\n        // Switch to the webview context\r\n        const webviewContext = contexts.find(c => c.includes('WEBVIEW'));\r\n        if (webviewContext) {\r\n            await driver.switchContext(webviewContext);\r\n\r\n            // Click the Auth0 button in the webview (native view selector)\r\n            const auth0Button = await $('//android.widget.TextView[@text=\"Auth0\"]');\r\n            await auth0Button.click();\r\n        } else {\r\n            throw new Error('WEBVIEW context not found');\r\n        }\r\n    });\r\n});\r\n"
        }
    ]
}